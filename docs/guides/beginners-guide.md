# CodeCoder 新手完全指南

欢迎使用 CodeCoder！这是一个开源的 AI 编程代理，可以帮助你编写、调试和管理代码。本指南将带你从零开始，逐步掌握 CodeCoder 的使用。

## 目录

1. [什么是 CodeCoder？](#什么是-codecoder)
2. [安装与配置](#安装与配置)
3. [第一次运行](#第一次运行)
4. [基础用法](#基础用法)
5. [常用功能](#常用功能)
6. [进阶技巧](#进阶技巧)
7. [常见问题](#常见问题)

---

## 什么是 CodeCoder？

CodeCoder 是一个 AI 编程代理，它可以：

- **编写代码** - 根据你的需求生成代码
- **调试代码** - 帮你找出并修复 bug
- **解释代码** - 解释现有代码的工作原理
- **重构代码** - 改进代码结构和可读性
- **执行命令** - 运行终端命令和脚本
- **搜索信息** - 在网络和代码库中查找信息

**支持的 AI 提供商**：Claude、GPT-4、Gemini、以及 20+ 其他提供商。

---

## 安装与配置

### 前置要求

CodeCoder 需要 **Bun 1.3+** 运行时。如果还没有安装 Bun：

```bash
curl -fsSL https://bun.sh/install | bash
```

验证安装：
```bash
bun --version
```

### 安装 CodeCoder

```bash
# 克隆仓库
git clone https://github.com/iannil/code-coder.git
cd code-coder

# 安装依赖
bun install
```

### 配置 AI 提供商

CodeCoder 需要至少一个 AI 提供商的 API 密钥才能工作。

> **详细配置**: 有关主流 AI 提供商的详细配置方法，请参阅 [主流提供商配置指南](./mainstream-providers.md)。
>
> **API Key 管理**: 有关如何安全地存储和管理 API Key，请参阅 [API Key 管理指南](./api-key-management.md)。

#### 获取 API 密钥

| 提供商 | 获取地址 | 推荐用于 |
|--------|----------|----------|
| Anthropic (Claude) | https://console.anthropic.com | 代码编写、推理 |
| OpenAI (GPT) | https://platform.openai.com | 通用任务 |
| Google (Gemini) | https://ai.google.dev | 免费方案 |
| Groq | https://groq.com | 快速推理 |

#### 设置密钥

```bash
# 登录并设置 API 密钥
codecoder auth login
```

或者手动创建配置文件：

**位置**: `~/.codecoder/config.json`

```json
{
  "provider": "anthropic",
  "model": "claude-sonnet-4-20250514",
  "apiKey": "sk-ant-your-api-key-here"
}
```

---

## 第一次运行

### 启动 CodeCoder

```bash
# 在当前目录运行
bun dev

# 或在指定目录运行
bun dev /path/to/your/project
```

### 你将看到

```
┌─────────────────────────────────────────────────────────────┐
│                    Welcome to CodeCoder!                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  CodeCoder is ready to help you with your coding tasks.    │
│                                                              │
│  Press [Space] to start a new conversation                 │
│  Press [?] to see all keyboard shortcuts                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 基础用法

### 1. 开始对话

最简单的使用方式 - 像聊天一样提问：

```
> 帮我写一个计算斐波那契数列的函数
```

CodeCoder 会：
1. 理解你的需求
2. 编写代码
3. 创建/修改文件
4. 解释代码的工作原理

### 2. 继续会话

```bash
# 继续上一次对话
codecoder -c

# 继续指定会话
codecoder -s session-id-here
```

### 3. 快速提问

```bash
# 直接在命令行提问
bun dev "帮我修复 login.js 中的错误"
```

### 4. 指定文件操作

```bash
# 让 AI 读取并分析特定文件
codecoder -f src/utils.js "解释这个文件的功能"
```

---

## 常用功能

### 📁 文件操作

#### 读取文件

```
> 读取 package.json 并列出所有依赖
```

#### 写入新文件

```
> 创建一个名为 helpers.js 的文件，包含日期格式化函数
```

#### 编辑文件

```
> 在 utils.js 中添加一个计算数组平均值的函数
```

#### 搜索代码

```
> 找出所有调用 fetchData 函数的地方
```

### 🖥️ 命令执行

```
> 运行测试并告诉我结果
> 安装 lodash 包
> 列出当前目录的文件
```

### 🔍 代码理解

```
> 解释这段代码的作用
> 这个函数有什么潜在问题？
> 如何优化这段代码的性能？
```

### 🐛 调试帮助

```
> 我的代码报错了：[粘贴错误信息]
> 为什么这个测试失败了？
> 帮我找出内存泄漏的原因
```

---

## 进阶技巧

### 1. 使用不同的 AI 模型

```bash
# 使用 GPT-4
codecoder -m openai/gpt-4 "写一个 Python 爬虫"

# 使用 Gemini
codecoder -m google/gemini-pro "分析这个数据集"

# 使用 Claude（默认）
codecoder -m anthropic/claude-sonnet-4 "重构这段代码"
```

### 2. 会话管理

```bash
# 列出所有会话
codecoder session list

# 查看最近的 5 个会话
codecoder session list -n 5

# 导出会话
codecoder session export <session-id>
```

### 3. 使用代理（Agent）

代理是预配置的专业助手，针对特定任务优化：

```bash
# 写作代理 - 适合编写文档和注释
codecoder --agent writer "为这个函数添加注释"

# 探索代理 - 适合理解代码库
codecoder --agent explorer "这个项目的主要功能是什么？"
```

### 4. 长文档写作

CodeCoder 支持分章节写作长文档：

```bash
# 创建新文档
codecoder document create

# 使用模板
codecoder document template use technical-blog

# 写入特定章节
codecoder document write <doc-id> --chapter "第一章：介绍"

# 导出完整文档
codecoder document export <doc-id> --format markdown
```

### 5. MCP 服务器

MCP（Model Context Protocol）允许扩展 CodeCoder 的能力：

```bash
# 列出可用的 MCP 服务器
codecoder mcp list

# 添加新的 MCP 服务器
codecoder mcp add

# 测试 MCP 连接
codecoder mcp debug <server-name>
```

### 6. Git 集成

```
> 创建一个新分支并切换
> 提交当前更改，消息为 "fix: 修复登录 bug"
> 查看 git 状态
> 创建 pull request
```

---

## 配置文件

### 项目级配置

在项目根目录创建 `codecoder.json`：

```json
{
  "provider": "anthropic",
  "model": "claude-sonnet-4-20250514",
  "temperature": 0.7,
  "maxTokens": 8192,
  "cwd": "/path/to/project",
  "exclude": [
    "node_modules/**",
    "dist/**",
    ".git/**"
  ]
}
```

### 全局配置

**位置**: `~/.codecoder/config.json`

```json
{
  "defaultProvider": "anthropic",
  "defaultModel": "claude-sonnet-4-20250514",
  "theme": "dark",
  "keybinds": {
    "leader": "space",
    "app_exit": "ctrl+q"
  }
}
```

---

## 键盘快捷键

### TUI 界面快捷键

| 按键 | 功能 |
|------|------|
| `Space` | 发送消息 |
| `Ctrl+C` | 取消当前操作 |
| `Ctrl+Q` | 退出应用 |
| `Ctrl+N` | 新建会话 |
| `Ctrl+L` | 列出会话 |
| `?` | 显示帮助 |
| `Ctrl+R` | 重新生成响应 |
| `Ctrl+E` | 编辑最后的消息 |

### 命令模式快捷键

| 按键 | 功能 |
|------|------|
| `Tab` | 自动补全 |
| `↑/↓` | 浏览历史命令 |
| `Ctrl+A` | 移到行首 |
| `Ctrl+E` | 移到行尾 |
| `Ctrl+U` | 删除到行首 |
| `Ctrl+K` | 删除到行尾 |

---

## 常见使用场景

### 场景 1：从零创建项目

```
> 创建一个 Node.js Express API 项目结构
> 添加用户认证功能
> 生成 README 文档
```

### 场景 2：代码审查

```
> 审查 src/auth.js 文件
> 指出潜在的安全问题
> 建议改进方案
```

### 场景 3：学习新代码库

```
> 这个项目的主要功能是什么？
> 画出项目的架构图
> 解释入口文件的工作流程
```

### 场景 4：重构代码

```
> 将这个回调函数转换为 async/await
> 提取重复代码到工具函数
> 添加 TypeScript 类型定义
```

### 场景 5：编写测试

```
> 为 utils.js 编写单元测试
> 使用 Bun 测试框架
> 确保覆盖所有边界情况
```

---

## 常见问题

### Q: 如何切换 AI 提供商？

```bash
# 临时切换
codecoder -m openai/gpt-4 "your message"

# 永久切换 - 编辑配置文件
# ~/.codecoder/config.json
{
  "defaultProvider": "openai",
  "defaultModel": "gpt-4"
}
```

### Q: API 密钥存储在哪里？

密钥存储在 `~/.codecoder/config.json`，确保此文件权限正确：
```bash
chmod 600 ~/.codecoder/config.json
```

### Q: 如何限制 CodeCoder 访问的文件？

在项目配置中使用 `exclude` 字段：
```json
{
  "exclude": [
    "node_modules/**",
    ".env",
    "**/*.secret"
  ]
}
```

### Q: 会话历史保存在哪里？

会话保存在 `~/.codecoder/sessions/` 目录下。

### Q: 如何分享会话？

```bash
# 生成分享链接
codecoder --share

# 导出会话
codecoder session export <session-id> --format markdown > session.md
```

### Q: CodeCoder 支持哪些编程语言？

所有主流编程语言：JavaScript、TypeScript、Python、Go、Rust、Java、C++、PHP、Ruby 等。

### Q: 如何获取帮助？

```bash
# 查看命令帮助
codecoder --help

# 查看特定命令帮助
codecoder session --help
codecoder auth --help

# 在 TUI 中按 ? 键
```

---

## 下一步

恭喜！你已经掌握了 CodeCoder 的基础用法。接下来可以：

1. **配置主流提供商** - 详细了解 Anthropic、OpenAI、Google 等提供商的配置
   - [主流提供商配置指南](./mainstream-providers.md)

2. **管理 API Key** - 学习如何安全地存储和管理 API 密钥
   - [API Key 管理指南](./api-key-management.md)

3. **配置自定义提供商** - 设置国内 AI 服务或自定义端点
   - [自定义提供商配置指南](./custom-provider.md)

4. **阅读架构文档** - 了解 CodeCoder 的内部原理
   - [Architecture-Guide.md](../Architecture-Guide.md)

5. **探索高级功能** - Skills、钩子系统、MCP 扩展
   - [Skills 进阶全景图](../Skills%20进阶全景图.md)

6. **参与贡献** - 在 GitHub 上提交问题或 PR
   - https://github.com/iannil/code-coder

7. **查看测试指南** - 学习如何为 CodeCoder 编写测试
   - [Testing Guide](./testing.md)

---

## 资源链接

- **GitHub**: https://github.com/iannil/code-coder
- **问题反馈**: https://github.com/iannil/code-coder/issues
- **更新日志**: [progress.md](../progress.md)

---

**祝你使用愉快！Happy Coding! 🚀**
