---
id: "release-checklist"
name: "发布检查清单"
version: "1.0.0"
schedule: "0 0 14 * * 5"
agent: "code-reviewer"
enabled: true
memory_path: "hands/release-checklist/{date}.md"
autonomy:
  level: "wild"
  unattended: false
  max_iterations: 5
decision:
  use_close: true
  web_search: false
  evolution: true
  auto_continue: false
resources:
  max_tokens: 80000
  max_cost_usd: 3.0
  max_duration_sec: 900
params:
  check_items:
    - "tests_passing"
    - "lint_clean"
    - "changelog_updated"
    - "version_bumped"
    - "docs_updated"
    - "no_console_logs"
    - "no_debug_code"
    - "security_scan"
    - "performance_check"
  required_approvers:
    - "tech-lead"
    - "qa-lead"
---

# 发布检查清单

每周五 14:00 执行发布前检查，确保代码质量符合发布标准。

## 职责

1. **自动检查**: 运行自动化检查项
2. **手动审核**: 协调需要人工确认的项目
3. **报告汇总**: 生成发布准备状态报告
4. **阻断决策**: 发现阻断性问题时阻止发布

## 检查清单

### 自动检查项

| 检查项 | 命令 | 阈值 |
|--------|------|------|
| 单元测试 | `bun test` | 100% 通过 |
| 类型检查 | `bun turbo typecheck` | 0 错误 |
| Lint | `bun lint` | 0 错误 |
| 构建 | `bun run build` | 成功 |

### 代码质量检查

- [ ] 没有 `console.log` 调试语句
- [ ] 没有 `// TODO` 或 `// FIXME` 遗留
- [ ] 没有 `any` 类型（除非必要）
- [ ] 没有注释掉的代码块
- [ ] 没有硬编码的测试数据

### 文档检查

- [ ] CHANGELOG.md 已更新
- [ ] 版本号已正确递增
- [ ] API 文档已同步
- [ ] 迁移说明已编写（如适用）

### 安全检查

- [ ] 无敏感信息泄露
- [ ] 依赖无已知漏洞
- [ ] 权限配置正确

## 输出格式

```markdown
# 发布检查报告 - v{version} - {date}

## 状态: 🟢 可以发布 / 🟡 需要关注 / 🔴 阻断

## 检查结果

| 检查项 | 状态 | 详情 |
|--------|------|------|
| 单元测试 | ✅ | 234/234 通过 |
| 类型检查 | ✅ | 0 错误 |
| 代码质量 | ⚠️ | 2 个警告 |
| 文档 | ✅ | 已更新 |
| 安全 | ✅ | 无问题 |

## 阻断问题
{如果有阻断性问题，列在这里}

## 警告事项
- {警告1}
- {警告2}

## 需要审批

以下人员需要确认发布：
- [ ] @tech-lead - 技术确认
- [ ] @qa-lead - 质量确认

## 发布步骤

确认后执行：
1. `git tag v{version}`
2. `git push origin v{version}`
3. 触发 CI/CD 流水线
```

## 自主级别说明

使用 `wild` 级别（部分自主），因为：
- 发布是高风险操作，需要人工最终确认
- 自动执行检查但不自动发布
- 设置 `auto_continue: false` 等待人工介入

## CLOSE 决策集成

发布决策评估：

| 维度 | 权重 | 评估内容 |
|------|------|----------|
| C (可用余量) | 15% | 团队带宽、时间窗口 |
| L (最大损失) | 35% | 发布失败的回滚成本 |
| O (成功概率) | 25% | 基于检查结果的成功率 |
| S (可持续性) | 15% | 对后续开发的影响 |
| E (进化空间) | 10% | 从发布中学习的机会 |

## 与 HITL 集成

当所有自动检查通过后，创建审批请求：

```yaml
approval_type:
  type: "risk_operation"
  description: "Release v{version} ready for deployment"
  risk_level: "High"
approvers:
  - "tech-lead"
  - "qa-lead"
```

两位审批者都确认后，可以继续发布流程。

## 使用说明

1. 复制到 `~/.codecoder/hands/release-checklist/`
2. 配置 `params.required_approvers` 为团队成员
3. 根据项目调整 `params.check_items`
4. 配置 HITL 的 IM 渠道接收审批请求
5. 可选：调整 `schedule` 匹配发布周期

## 扩展

可以添加自定义检查项：

```yaml
params:
  custom_checks:
    - name: "E2E Tests"
      command: "bun run test:e2e"
      required: true
    - name: "Bundle Size"
      command: "bun run analyze"
      threshold: "500kb"
```
