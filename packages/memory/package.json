{
  "$schema": "https://json.schemastore.org/package.json",
  "version": "0.0.1",
  "name": "@codecoder-ai/memory",
  "type": "module",
  "license": "MIT",
  "private": true,
  "description": "Unified memory module supporting SQLite and Markdown backends",
  "scripts": {
    "typecheck": "tsgo --noEmit || tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage"
  },
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "peerDependencies": {
    "bun": ">=1.3.0"
  }
}
